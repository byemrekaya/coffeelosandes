# İletişim formu — sunucu tarafı SMTP (gizli; repoya commit etmeyin)
SMTP_HOST=smtp.ornek.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=postmaster@ornek.com
SMTP_PASS=
# İsteğe bağlı gönderen başlığı (SPF/DKIM ile uyumlu bir adres kullanın)
# SMTP_FROM=Coffee Los Andes <noreply@ornek.com>
# Alıcı farklıysa (varsayılan: site.contactEmail)
# MAIL_TO=info@coffeelosandes.com
